/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      // Design tokens will be added in Phase 1
      //Colors, spacing, radii, shadows, typography scales
    },
  },
  plugins: [],
};
